<div class="container">
  <div class="row">
    <div class="col-lg-10"><h2>Hike Results:</h2></div>
    <div clas="col-lg-1"><button class="btn active"><i class="fas fa-list"></i></button></div>
    <div clas="col-lg-1"><button class="btn active"><i class="far fa-map"></i></button></div>
  </div>

  <% @hikes.each do |hike| %>
      <% if hike.photos.attached? %>
        <%= link_to hike_path(hike), class:'text-decoration-none' do %>
          <div class = "col-lg-12">
            <div class="card-product">
              <%= cl_image_tag hike.photos.first.key, class: 'hike-image' %>
                <div class="card-product-infos">
                  <div>$<%=hike.price%></div>
                 <div><h2><%= hike.name %></h2></div>
                  <div><p><em><%= hike.region %></em></p></div>
                  <div><i class="fas fa-arrow-right"></i> <%= hike.distance %> Kilometers</div>
                  <div><i class="fas fa-arrow-up"></i> <%= hike.elevation %>m Elevation</div>
                  <div><%= hike.difficulty %></div>
                </div>
            </div>
            <% end %>
        </div>
      <% else %>
        <%= link_to hike_path(hike), class:'text-decoration-none' do %>
          <div class = "col-lg-12">
            <div class="card-product">
              <img src="https://images.unsplash.com/photo-1556653922-ca7aa575c39f?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&ixid=MnwxfDB8MXxhbGx8fHx8fHx8fHwxNjIxNjAwODE3&ixlib=rb-1.2.1&q=80&w=1080&utm_source=unsplash_source&utm_medium=referral&utm_campaign=api-credit" />
              <div class="card-product-infos">
                  <div>$<%=hike.price%></div>
                 <div><h2><%= hike.name %></h2></div>
                  <div><p><em><%= hike.region %></em></p></div>
                  <div><i class="fas fa-arrow-right"></i> <%= hike.distance %> Kilometers</div>
                  <div><i class="fas fa-arrow-up"></i> <%= hike.elevation %>m Elevation</div>
                  <div><%= hike.difficulty %></div>
                </div>
            </div>
            <% end %>
          </div>
      <% end %>
       <% end %>
</div>       


<div class="container">
    <div class = "row">
      <div class="col-lg-12">
      <div id="map"
          style="width: 100%; height: 600px;"
          data-markers="<%= @markers.to_json %>"
          data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
      </div>
    </div>
  </div>